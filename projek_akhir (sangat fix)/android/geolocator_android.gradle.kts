// Konfigurasi override untuk plugin geolocator_android
project.extra.apply {
    set("compileSdkVersion", 35)  // Tetapkan compileSdkVersion
    set("minSdkVersion", 21)
    set("targetSdkVersion", 35)
}